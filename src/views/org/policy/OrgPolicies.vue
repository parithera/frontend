<script lang="ts" setup>
import ErrorComponent from '@/common_components/ErrorComponent.vue';
import LoadingComponent from '@/common_components/LoadingComponent.vue';
import { defineAsyncComponent } from 'vue';

const OrgPoliciesList = defineAsyncComponent({
    loader: () => import('@/views/org/policy/OrgPoliciesList.vue'),
    loadingComponent: LoadingComponent,
    // Delay before showing the loading component. Default: 200ms.
    delay: 200,
    errorComponent: ErrorComponent,
    // The error component will be displayed if a timeout is
    // provided and exceeded. Default: Infinity.
    timeout: 3000
});

const OrgPoliciesCreate = defineAsyncComponent({
    loader: () => import('@/views/org/policy/OrgPolicyCreate.vue'),
    loadingComponent: LoadingComponent,
    // Delay before showing the loading component. Default: 200ms.
    delay: 200,
    errorComponent: ErrorComponent,
    // The error component will be displayed if a timeout is
    // provided and exceeded. Default: Infinity.
    timeout: 3000
});

const OrgPoliciesEdit = defineAsyncComponent({
    loader: () => import('@/views/org/policy/OrgPolicyEdit.vue'),
    loadingComponent: LoadingComponent,
    // Delay before showing the loading component. Default: 200ms.
    delay: 200,
    errorComponent: ErrorComponent,
    // The error component will be displayed if a timeout is
    // provided and exceeded. Default: Infinity.
    timeout: 3000
});

defineProps<{
    page: string;
    orgId: string;
    action?: string;
}>();
</script>
<template>
    <OrgPoliciesList v-if="action == 'manage'" :page="page" :orgId="orgId" />
    <OrgPoliciesCreate v-else-if="action == 'add'" :page="page" :orgId="orgId" />
    <OrgPoliciesEdit v-else-if="action == 'edit'" :page="page" :orgId="orgId" />
</template>
