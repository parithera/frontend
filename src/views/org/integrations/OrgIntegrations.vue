<script lang="ts" setup>
import ErrorComponent from '@/common_components/ErrorComponent.vue';
import LoadingComponent from '@/common_components/LoadingComponent.vue';
import { defineAsyncComponent } from 'vue';

const OrgIntegrationsList = defineAsyncComponent({
    loader: () => import('@/views/org/integrations/OrgIntegrationsList.vue'),
    loadingComponent: LoadingComponent,
    // Delay before showing the loading component. Default: 200ms.
    delay: 200,
    errorComponent: ErrorComponent,
    // The error component will be displayed if a timeout is
    // provided and exceeded. Default: Infinity.
    timeout: 3000
});

const OrgIntegrationCreate = defineAsyncComponent({
    loader: () => import('@/views/org/integrations/OrgIntegrationCreate.vue'),
    loadingComponent: LoadingComponent,
    // Delay before showing the loading component. Default: 200ms.
    delay: 200,
    errorComponent: ErrorComponent,
    // The error component will be displayed if a timeout is
    // provided and exceeded. Default: Infinity.
    timeout: 3000
});

const OrgIntegrationEdit = defineAsyncComponent({
    loader: () => import('@/views/org/integrations/OrgIntegrationEdit.vue'),
    loadingComponent: LoadingComponent,
    // Delay before showing the loading component. Default: 200ms.
    delay: 200,
    errorComponent: ErrorComponent,
    // The error component will be displayed if a timeout is
    // provided and exceeded. Default: Infinity.
    timeout: 3000
});

defineProps<{
    page: string;
    orgId: string;
    action?: string;
}>();
</script>
<template>
    <OrgIntegrationsList v-if="action == 'manage'" :page="page" :orgId="orgId" />
    <OrgIntegrationCreate v-else-if="action == 'add'" :page="page" :orgId="orgId" />
    <OrgIntegrationEdit v-else-if="action == 'edit'" :page="page" :orgId="orgId" />
</template>
